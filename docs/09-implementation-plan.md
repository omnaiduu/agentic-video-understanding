# Implementation plan

**Superseded.** The build order is the **12 locked phases**.

Read:

1. [12 — Build phases](12-build-phases.md) — map
2. [phases/phase-01.md](phases/phase-01.md) through [phase-12.md](phases/phase-12.md) — one slice at a time
3. [13 — Implementation pass](13-implementation-pass.md) — libraries / Modal / size (**open** — do not invent GPUs)

Do not implement from the old 7-step skeleton (SQLite, 12B native tools, Vite-only). That list is history.

**Done when** (unchanged product bar): from the website, a ≥10 minute video can (a) answer a speech question, (b) a silent visual, (c) a sound question, (d) a follow-up without re-ingest, (e) show an exported clip **in the chat**, (f) work on a phone — without loading the whole file into Gemma.
